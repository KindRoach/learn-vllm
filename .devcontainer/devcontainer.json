{
  "name": "learn-vllm",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "mounts": [
    "source=/models,target=/models,type=bind,consistency=cached,readonly"
  ],
  "runArgs": [
    "--runtime=nvidia",
    "--gpus=all",
    "--shm-size=16G"
  ]
}